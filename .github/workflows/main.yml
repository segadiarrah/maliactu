on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20' # Update this line to use Node.js 20

      # Add any steps that require Node.js below this line.
      # For example, if you need to install dependencies:
      - name: Install dependencies
        run: npm install
        # Make sure this step is needed for your project

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 179.61.246.218
          username: admin_cloudflare
          password: ${{ secrets.ftp_password }}
          server-dir: public_html/
